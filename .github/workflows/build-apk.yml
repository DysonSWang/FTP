name: Build Android APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: '22'

    - uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install Dependencies
      run: npm install

    - name: Setup Android
      uses: android-actions/setup-android@v3

    - name: Install Capacitor
      run: npm install @capacitor/core @capacitor/cli @capacitor/android

    - name: Create www directory
      run: |
        mkdir -p www
        cp index.html www/index.html
        cp policies-data.js www/policies-data.js
        cp knowledge-base.js www/knowledge-base.js

    - name: Initialize Capacitor
      run: |
        cat > capacitor.config.json << 'EOF'
        {
          "appId": "com.hainan.ftp.policy",
          "appName": "海南自贸港政策库",
          "webDir": "www",
          "server": {
            "url": "http://120.48.16.193:5173",
            "cleartext": true
          },
          "android": {
            "allowMixedContent": true
          }
        }
        EOF

    - name: Add Android Platform
      run: |
        rm -rf android
        npx cap add android

    - name: Sync
      run: npx cap sync android

    - name: Fix Java Version in android/build.gradle
      run: |
        cd android
        echo "=== 修复项目级 build.gradle ==="
        sed -i 's/VERSION_21/VERSION_17/g' build.gradle
        sed -i 's/VERSION21/VERSION17/g' build.gradle
        sed -i "s/= '21'/= '17'/g" build.gradle
        sed -i 's/= "21"/= "17"/g' build.gradle
        cat build.gradle | grep -i "version" || true

    - name: Fix Java Version in android/app/build.gradle
      run: |
        cd android/app
        echo "=== 修复 app 级 build.gradle ==="
        
        # 显示原始配置
        echo "=== 原始 Java 配置 ==="
        grep -n "VERSION_\|sourceCompatibility\|targetCompatibility" build.gradle || echo "未找到"
        
        # 替换所有 Java 21 为 Java 17
        sed -i 's/VERSION_21/VERSION_17/g' build.gradle
        sed -i 's/VERSION21/VERSION17/g' build.gradle
        sed -i "s/= '21'/= '17'/g" build.gradle
        sed -i 's/= "21"/= "17"/g' app/build.gradle 2>/dev/null || true
        sed -i 's/sourceCompatibility 21/sourceCompatibility 17/g' build.gradle
        sed -i 's/targetCompatibility 21/targetCompatibility 17/g' build.gradle
        
        # 验证修改
        echo "=== 修改后的 Java 配置 ==="
        grep -n "VERSION_\|sourceCompatibility\|targetCompatibility" build.gradle || echo "未找到"

    - name: Create gradle.properties
      run: |
        cd android
        echo "" >> gradle.properties
        echo "org.gradle.java.home=/opt/hostedtoolcache/Java_Temurin-Hotspot_jdk/17.0.18-8/x64" >> gradle.properties
        echo "org.gradle.jvmargs=-Xmx2048m" >> gradle.properties
        cat gradle.properties

    - name: Build APK
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: hainan-ftp-policy-app
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
